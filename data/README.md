# Data Directory

Keep private and large datasets outside Git.

- `data/sample/` may contain tiny synthetic or anonymized fixtures.
- `data/raw/`, `data/external/`, and `data/production/` are ignored.
